---
interface Props {
  currentPath?: string;
}

const { currentPath = "/" } = Astro.props;
const isHomePage = currentPath === "/";
const isDraftsPage = currentPath === "/drafts.html" || currentPath.startsWith("/drafts/");
const isAiMathPage = currentPath === "/ai-assisted-math.html";
---

<header class="top-nav">
  <div class="shell nav-shell">
    <a class="brand-link" href="/" aria-current={isHomePage ? "page" : undefined}>Home</a>
    <nav class="nav-links" aria-label="Main navigation">
      <a href="/drafts.html" aria-current={isDraftsPage ? "page" : undefined}>Drafts</a>
      <a href="/ai-assisted-math.html" aria-current={isAiMathPage ? "page" : undefined}>AI-assisted math</a>
    </nav>
  </div>
</header>
